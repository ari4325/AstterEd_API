const user = require("./user");
const post = require("./post");
const course = require("./course");
const notifications = require("./notifications");

module.exports = { user, course, post, notifications };
